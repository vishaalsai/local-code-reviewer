You are a code review assistant. Analyze the following {language} code{file_info} and respond with ONLY a valid JSON object — no markdown, no explanation, no code fences, just raw JSON.

The JSON must match this exact schema:
{{
  "summary": "one sentence describing the overall code quality",
  "issues": [
    {{
      "line_number": <integer>,
      "severity": "<low|medium|high|critical>",
      "description": "<what is wrong and why>"
    }}
  ],
  "suggestions": [
    "<actionable improvement suggestion>"
  ],
  "overall_severity": "<low|medium|high|critical>"
}}

Rules:
- severity must be exactly one of: low, medium, high, critical
- line_number must be an integer (use 0 if not applicable)
- issues and suggestions must be arrays (can be empty but not omitted)
- Do NOT wrap the JSON in markdown code blocks
- Do NOT add any text before or after the JSON

Code to review:
```{language}
{code}
```
